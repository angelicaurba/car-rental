"use strict"

class User {
    constructor(id, username, password, name){
     this.id = id;
     this.username = username;
     this.password = password;
     this.name = name;
    }
}

module.exports = User;